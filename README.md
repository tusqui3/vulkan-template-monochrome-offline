# vulkan-RayTracing-Template-Monochrome-Offline

Offline ray tracer using `VK_KHR_ray_tracing_pipeline`. Renders the typical triangle. Purpose is to serve as a template for further projects, and to the understanding of the vulkan pipeline.

Needs RTX GPU. 

Uses header only png export tool stb.
